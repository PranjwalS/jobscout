1. setup list of proper keywords to be provided, it'll be like checkboxes, used durin dashboard creation by the user, 
so basically keep a limited set of keywords to work with. 
split into four parts; skills && fields && EXCLUDES
skills; ie; react, python, c++....
fields; ie; data, ai, full stack....
exclude skills; ie; swimming, driver's liscense....
exclude fields: ie; marketing, business, tax, QA testing....
    per user, add this to dashboard_configs schemas


from there;
back in scraper logic;
    create 3 sets basically -> SKILLS and FIELDS
                        and -> LOCATIONS
        
        and it basically just goes through all the dashboard configs across all users basically, and just 
        appends all to these two sets while deduplicating so like it's a SET.
    and then we begin the actual scraper logic basically as in; take the LOCATIONS and size them out to their respective countries;
    and just run scraper country by country basically; simple static dict to map whatever the user inputs up to country level for the scrape matrix. 
    and also; 
    def get_country_depth(country, user_count):
        base = 250
        return min(base * max(1, user_count), 5000) // to figure out how many limits per country basically.
    
    FIELD_SUPERSET = {
    "ai": "software", "ml": "software",
    "full stack": "software", "backend": "software",
    "data engineer": "software", "devops": "software",
    "mechanical": "engineering", "embedded": "engineering",
    }

    dedup logic {
        if url exists → skip
        elif title + company match: 
            do desc_similarity = ...
            if exact location match AND desc similarity > 0.85 → replace the prev version with the reposted (id search) AND downwards cascade for ppl that already applied
            elif diff location AND desc similarity > 0.85 -> merge locations
            else -> new row
        else → new row

        AND
        at the start of every run just pull all existing (id, url, title, company, location) tuples into memory as a set/dict. then all dedup checks are in-memory lookups, zero db hits mid-scrape:    
            existing = supabase.table("jobs").select("id, url, title, company, locations").execute()
            url_set = set(row["url"] for row in existing.data)
            title_company_map = {
                (row["title"], row["company"]): row 
                for row in existing.data
            }
    }

    NOW FOR the main job by job logic; 
        We run the scraper a given set every few hours
        We also set up the search matrix using the fields, we find the superset of fields that englobe chosen fields, and separate them into their own searches; ie; sw, hw, eng, med... AND also job_type comes in here
        So for locations, the logic ensures we are already scraping inside a valid country and this gets paired with the search keywords identified basically. So this is SET #4 of keyword/location/job_type pairs
        Layer 1 => Check for at least one of the fields present in the title
        Layer 2 => We fuzzy search through title + company + url + location (many locations coming in for the same position, we'll add all, but ig we'll indicate all available locations as one job with a dropdown in dashboards) to avoid duplicates in the table, and skip if already existant, or we replace it with this new version.
        Layer 3 => Check through title + description with smart logic for fields and skills; any that match are officially considered relevant for the jobs schema
        Layer 4 => Just while checking for fields and skills in layer 3, also do a quick check for anything regarding 1. Salary and 2. Job Requirements and 3. Job start/length/season (for co-op students especially)
        Layer 5 => Push the selected jobs to the jobs schema, and in the columns relevant, for every job, pitch in the identified skills, identified fields, as well as misc column for salary, season info, and requirements info
                        to avoid re going through job info later on for this.

    Move into the next pair/trio in the job search matrix.

    So the flow is; 
    
        Pull from supabase all the relevant info every run
        Setup scrape time logic (jobs posted in the last XXX minutes) and a request delay between requests to avoid ratelimits
        Create 3 sets 
        Map locations to countries and fields to broader categories
        Create a 4th set for searches involving identified countries AND categories AND job_type
        Setup limits logic allowed per search trio in the 4th set to optimize overall (get_country_depth , etc)
        
        Run a scrape on every search trio, while counting the number of scraped as in while scraped <= limits[search_idx] && search_idx <= len(limits):
            start scrape logic based on info;
            if true in "Check for field in title":
                if true in "Fuzzy search dedup":
                    check for for skills, fields, salary, season, requirements through title + desc
                    if relevant:
                        fetch all info and map everything to write columns
                        add to supabase jobs schemas.
                    
                    else: 
                        continue
                else:
                    continue
            else:
                continue
            ++1 on scraped count
        Continue until all searches depleted
        
        Done?






----


Changes;

new pipeline; scraper gets meatier;

    scraper → passes layer 5 → insert into jobs → push job_id to Redis queue → continue

    separate Celery worker (always running) → pulls from Redis queue → GIN query → batch insert user_jobs
                                                                    -> GIN query checks every passing job, across all dashboard_configs available.

revisit fields.json; refactor as in, make the subfields a little broader but maintain the {superfield: field} form as is currently

make sure that in layer 3 + 4; that we create sets that basically fetch all the relevant data from the title + desc and redistribute them; go back into fields and skills.json and fetch all

layer 5 has also its own basically uhh spot in the db but its basically the last step of the flow, gets called with all the data on the job + all the sets from layer3+4; and then just adds to jobs schema
        layer 5 to do the full updates; called once per inner while loop in run()
